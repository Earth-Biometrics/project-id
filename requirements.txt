# Earth-Biometrics / project-id — Python dependencies
#
# Install into the project virtual environment:
#   python -m venv .venv
#   source .venv/bin/activate
#   python -m pip install --upgrade pip
#   python -m pip install -r requirements.txt
#
# Versions below match the working .venv (Python 3.13, macOS/Apple Silicon).

# --- Notebook / tooling ----------------------------------------------------
ipykernel==7.3.0
matplotlib==3.11.1
# ipywidgets enables tqdm progress bars (e.g. MegaDetector weight downloads);
# without it Jupyter logs a harmless "IProgress not found" warning.
ipywidgets==8.1.9

# --- Core scientific stack -------------------------------------------------
numpy==2.5.2
pandas==3.0.5
pillow==12.3.0

# --- Deep learning runtime -------------------------------------------------
torch==2.13.0
torchvision==0.28.0

# --- MegaDetector v6 (notebooks 01 & 02) -----------------------------------
# Animal/person/vehicle detection in camera-trap imagery.
# MegaDetector v6 is the vendored checkout at third-party/eb_MegaDetector_v6
# (the `megadetector_core` package, built on PyTorch-Wildlife). Install it in
# editable mode; it pulls in PytorchWildlife/ultralytics via its pyproject.toml:
#   python -m pip install -e third-party/eb_MegaDetector_v6
# Notebook 01 (01_setup_megadetector.ipynb) performs this install for you.
#
# PyTorch-Wildlife imports these audio libraries at package-load time (its
# bioacoustics module) even for image-only detection, but does not declare them:
soundfile==0.14.0
librosa==1.0.0

# --- SpeciesNet species classifier (notebooks 04 & 05) ---------------------
# Google cameratrapai; the vendored checkout is third-party/eb_cameratrapai
# (the `speciesnet` package). Install it in editable mode; it pulls in yolov5,
# onnx2torch, kagglehub, reverse_geocoder, etc. via its pyproject.toml:
#   python -m pip install -e third-party/eb_cameratrapai
# Notebook 04 (04_setup_speciesnet.ipynb) performs this install for you. On first
# use it downloads the model (~214 MB) from Kaggle; no Kaggle login is required.

# --- BioCLIP-2 species identification (notebook 03) ------------------------
# pybioclip is the official inference wrapper; its default model is
# hf-hub:imageomics/bioclip-2. It pulls in open_clip_torch, timm and
# huggingface-hub (pinned here for reproducibility).
pybioclip==2.1.6
open_clip_torch==3.3.0
timm==1.0.28
huggingface-hub==1.28.0
